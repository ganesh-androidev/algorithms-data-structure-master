class FindMissingNumInArray {



    public static void main(String arg[]) {
    // Arrays Should Not Contain duplicates
    // Arrays No need to be sorted order
    // Value should be in a range

        int array[] = {1,2,4,5};
        int sum1 = 0;

        for (int i=0;i< array.length;i++) {
            sum1 = sum1 + array[i]
        }
        
        int sum2 =0;

        for (int i = 1
        System.out.println(" Sum of Elements in Given Array :" + sum2);
    
    }
}
